'use strict';

/**
 * @ngdoc function
 * @name myappApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the myappApp
 */

APP.controller('MainCtrl', function ($scope) {
    // $scope.menuItems = [
    //   {message:'HTML5 Boilerplate', action:'go to page whatever', value:381},
    //   {message:'AngularJS', action:'go to page whatever'},
    //   {message:'Karma', action:'go to page whatever'}
    // ];

    // $scope.buttonAction = function() {

    // 	console.log(this); //is the button that was clicked

    // 	//this.item is the object in the ng-repeat loop we were currently on when making the button

    // 	APP.results.push(this.item);

    // 	console.log(APP.results);
    // }
  });
